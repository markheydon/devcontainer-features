# Contributing

PRs welcome.

## Adding a Feature

1. Create a new folder under `src/<feature-name>`
2. Add:
   - devcontainer-feature.json
   - install.sh
3. Ensure install.sh is idempotent

## Testing

Use a devcontainer locally to validate.